Для того, чтобы запустить каждый код, необходимо запустить модули _ _name_ _ == “_ _main_ _”

1 задание
Первый код cleaner обрабатывает данные по 1,4,5,6,7 типу невалидной геометрии (самопересечение, нулевая площадь, неправильная ориентация, вырожденные отверстия, пустая геометрия) 
make_valid() из shapely берёт сломанный полигон и разрезает его в точке самопересечения на два корректных. Иногда результат — не Polygon, а GeometryCollection (набор разных фигур). Тогда buffer(0) нормализует это обратно в нормальный полигон — это стандартный трюк в геоинформатике, потому что операция «расширить контур на 0 метров» пересчитывает топологию заново и выбрасывает мусор.
Двухступенчатость важна: make_valid решает 95% проблем, buffer(0) добивает оставшиеся 5%.

какие улучшения по сравнению с решением МТС:
•	удаляются точные дубликаты геометрии (побайтовое сравнение wkb)
до удаления в А – 171454. После – 163767
до удаления в Б – 161076. После – 160424

•	площадь считается в метрической проекции (UTN 36B, EPSG:32636, не в градусах)

2 задание
Спкрипт направлен на сопоставление объектов. код принимает очищенные данные и сопоставляет объекты из двух источников к одному физ зданию 

какие улучшения по сравнению с решением МТС:

•	двухступенчатый матчинг: IoU + центральное расстояние – визуализируем (метрики Жаккара) 
Кросс-матчинг: 143044 совпадений, avg IoU: 0.3637874395830212

•	адресный матчинг 
Адресов в A: 18239, ключей в B: 32604
Адресных совпадений: 5915

•	Дополнительны проход.
Пробегаем по трем критериям, оставшиеся данные 
1)	Hausdorff distance (близкие контуры)
2)	Соотношение площадей (похожий размер)
3)	Расстояние центроидов
Необходимо, чтобы 2 критерия минимум выполнялись, тогда данные сопоставляются дополнительно.

•	улучшенная кластеризация (буфер 0,5м для касания, IoU > 0,05 для определения частей здания, не кластеризуем, если площади слишком разные 
Кластеризация A: 163767 полигонов (Кластеров: 159230 (многополигонных: 3570)). Из них near-duplicates (IoU > 0.8): 250 пар
Кластеризация B: 160424 полигонов (Кластеров: 159899 (многополигонных: 368)). Из них near-duplicates (IoU > 0.8): 36 пар

•	граф связности с весами и метаданными 
у графа 324191 вершин, 162348 ребер 
ребра: intra_a=6316, intra_b=768, cross=152739, address=2525
Компоненты: 170363 (только A: 42348, только B: 34298, совпали: 93717)

•	используется бимодальное распределение (двухступенчатая фильтрация выбросов) (наши два горба с двумя медианами) 


3 задание
в нем реализованная млька, в которой мы рассмотрели 5 разных подходов (Приоритетный источник, Простое усреднение, Взвешенное усреднение по надежности и площади, голосование, ML-предсказание)

выбрали из них комбинацию из 3-4-5 
и начали обучать модель. для каждого здания определяем в итоге финальную высоту по алгоритму 3х оценок. 
	⁃	3 оценки: взвешенная медиана ([14,14,50] среднее = 26,3 плохо, медиана = 15 супер!).  Количество - 16076
	⁃	2 оценки: взвешенное среднее. Количество - 108096
	⁃	1 оценка: берем как есть. Количество - 3465
	⁃	0 оценок: летит в ML. Количество – 42726
Средняя площадь: 215 м²
Медианная высота: 16.9 м
так же мы добавили разрешение конфликтов с соседями. смотрим на высоту соседей и выбираем ту оценку, которая ближе к окружению ([15,40] соседи в среднем 14, берем 15!)
Корреляция высоты с соседями: 0.7268
Вердикт: все хорошо - заметная корреляция (на 167296 зданиях).
0.73 >0.7 результат адекватный  

Подход A: «Приоритетный источник» (baseline МТС)
  Идея: выбрать один «лучший» источник и брать высоту оттуда.
  Плюсы: простота, скорость.
  Минусы: полностью игнорирует данные из других источников.
  Если приоритетный источник ошибся - ошибка не ловится.
  ВЫВОД: ОТКЛОНЕН как основной метод, но используется как fallback.

Подход B: «Простое усреднение»
  Идея: если есть несколько оценок - взять среднее.
  Плюсы: использует все данные.
  Минусы: одна грубая ошибка (выброс) искажает все среднее.
  Не учитывает что источники имеют разную надежность.
  Вывод: ОТКЛОНЕН.

Подход C: «Взвешенное усреднение по надежности и площади» (НАШ ВЫБОР)
  Идея: каждой оценке высоты назначается вес, который зависит от:
    1) Надежности источника (инструментальное измерение > расчЕт > грубая оценка)
    2) Площади полигона (больший полигон = более детальная геометрия = надежнее)
    3) Согласованности с другими оценками (если все говорят +-15 м, а один - 50 м,
       у выброса вес снижается)

Подход D: «Голосование» (дополнение к C)
  Идея: если есть 3+ независимых оценки - берем взвешенную медиану.
  Медиана устойчива к выбросам (1 грубая ошибка не влияет).
  Вывод: ИСПОЛЬЗУЕТСЯ при наличии 3+ оценок.

Подход E: «ML-предсказание» (дополнение к C)
  Идея: для зданий без данных - обучить модель на зданиях с известной высотой.
  Фичи: площадь, форма, высота соседей, назначение.
  Вывод: ИСПОЛЬЗУЕТСЯ как fallback для зданий без высоты из источников.

ИТОГОВЫЙ АЛГОРИТМ (комбинация C + D + E):


4задание

В этом задании мы проверяем, что результат, который мы получили вообще разумен. у нас нет правильных ответов, но нужно провести косвенную проверку, через несколько независимых проверок.
6 проверок:
Статистическая проверка (медиана должна быть +-10м)
	⁃	зданий с высотой выше 100м единицы 
Зданий >100 м: 1  
	⁃	здания ниже 3м =заборы/пристройки - не берем
Зданий <3 м: 13 (0.0%)
Согласование источников 
	⁃	у 120к зданий данные из А и Б совпадают (CV <15%)
		Сравнение height(Б) vs gkh×3(А) для 15657 зданий:
	⁃	если 90% зданий согласованны в пределах 1 этажа - отлично 
		Расхождение <3 м (1 этаж): 8338 (53.3%). Не самый лучший результат, но в пределах разумного.


ML-Модель: Holdout test 
	⁃	мы обучили 80%, проверили на 20% (которые модель не увидела) 
Данные: 127123 зданий с height_b
	⁃	MAE, RMSE, R^2 - метрики на этих 20%
MAE: 2.53 м (средняя ошибка)
RMSE: 4.98 м
R²: 0.7981
	⁃	дополнительная гистограмма: MAE <3м = ошибка меньше 1 этажа = хорошо 
MAE: 2.53 м (средняя ошибка)
Ошибка <1 этажа (3 м): 74.0% зданий
Ошибка <2 этажей (6 м): 88.3% зданий
Пространственная проверка 
	⁃	средняя высота должна быть разной(центр(15-20м), спальные районы(30-50м), промзоны(5-10м))


Проверка соседей 
	⁃	берем случайные значения здания и смотрим: похожа ли их высота на соседей resolved_by_neighborыs: 972 (0.6%)
	⁃	считаем корреляцию: высота зданий vs средняя высота соседей в 100м (> 0,7 = модель адекватная) Корреляция 0.73
Анализ ошибок ML. 
	⁃	группируем по: размеру, назначению, кол-ву оценок (понимаем слабые места). если конкретно здания с low confidence - это наши слабые места.   распределение по уверенности. Здания с низкой уверенностью: 42767 (25.1%). 
ML ошибка 2.53 м <3 м (меньше 1 этажа)
Покрытие: 170363 зданий (100%)

5 задание
скачиваем данные о зданиях Санкт-Петербурга из OSM (overpass api). 
	⁃	мы используем 3 сервера, если один падает - используем другой
извлекаем этажность (она есть примерно для 30-40%). пространственно сопоставляем OSM-здания с нашими IoU. добавляем третий голос для решения конфликтов. 

до использования кода. у нас 2 источника, добавляем 3тий - OSM. 
	⁃	медиана становится надежнее 
	⁃	перекрестная валидации: если все 3 согласны - уверенность максимальна 
	⁃	для конфликтных зданий (1007) - OSM может быть решающим голосом
но ставим ограничения!!!
OSM - это не МЕГАДУПЕРСУПЕР точный источник, данные заполнены волонтерами, не все здания имеют этажность. в нашем случае качество отличное, но если территории не популярная, то нужна проверка качества. геометрия из OSM модет отличаться от нашей 

по коду. 

функция parse_osm_to_gdf превращает сырой json в GeiDataFrame, далее OSM хранит данные в виде: 
	⁃	Nodes (точки): id, lat, lon
	⁃	Ways (линии, полигоны): id, nodes_ids, теги
	⁃	Relations (сложные объекты): id, список members. теги

функция match_osm_to_buildings. пространственно сопоставляет данные из наших источников и OSM, через центройды и площадь.

далее osm_obstacle 
мы в нем качаем данные о памятниках, вышках, мачтах, мостах, кранах, опорах ЛЭП, колоннах) 
зачем делаем - обьекты не являются зданиями, но физ блокируют/отражают сигнал. 

далее реализуем карту в visualize_map
строится 4 карты. центральная, приморский, кировский и тепловая карта всего города.



